Bem-vindo ao aluga temporada!

<%= current_realtor.name %>

<%= form_tag("/search_region", method: "get") do %>
  <%= label_tag(:'q[region]', "Região:") %>
  <%= collection_select(:q, :region, Region.all, :id, :name) %>
  <%= submit_tag("Buscar") %>
<% end %>


