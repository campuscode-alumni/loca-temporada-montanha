<h1>Editar região</h1>

<ul>
<% @region.errors.full_messages.each do |message| %>
  <li><%= message %></li>
<% end %>
</ul>

<%= form_for @region do |f| %>
  <%= f.label :name, 'Título' %>
  <%= f.text_field :name %>
  <%= f.submit 'Salvar' %>
<% end %>